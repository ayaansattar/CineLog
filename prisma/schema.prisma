generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Section {
  id        String   @id @default(cuid())
  name      String
  mediaType String   @default("movie")
  /// watchlist | watching — headings are per status + media type
  status    String   @default("watchlist")
  sortOrder Int      @default(0)
  createdAt DateTime @default(now())
  entries   Entry[]

  @@index([mediaType, status])
}

model Entry {
  id                 String    @id @default(cuid())
  tmdbId             Int?
  title              String
  year               Int?
  mediaType          String
  posterPath         String?
  genres             String?
  overview           String?
  status             String
  rating             Float?
  notes              String?
  currentSeason      Int?
  currentEpisode     Int?
  progressMark       String?
  progressUpdatedAt  DateTime?
  addedAt            DateTime  @default(now())
  watchedAt          DateTime?
  sectionId          String?
  sectionOrder       Int       @default(0)
  section            Section?  @relation(fields: [sectionId], references: [id], onDelete: SetNull)

  @@index([status])
  @@index([title, year])
  @@index([sectionId])
}
